<?xml version="1.0" encoding="utf-8" standalone="yes" ?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>My Academic</title>
    <link>https://cauliyang.github.io/en/</link>
      <atom:link href="https://cauliyang.github.io/en/index.xml" rel="self" type="application/rss+xml" />
    <description>My Academic</description>
    <generator>Wowchemy (https://wowchemy.com)</generator><language>en-us</language><copyright>© 2020 Yangyang Li. All Rights Reserved.</copyright><lastBuildDate>Thu, 10 Jun 2021 13:38:00 +0800</lastBuildDate>
    <image>
      <url>https://cauliyang.github.io/media/sharing.jpeg</url>
      <title>My Academic</title>
      <link>https://cauliyang.github.io/en/</link>
    </image>
    
    <item>
      <title>Cofiguration of My Mac</title>
      <link>https://cauliyang.github.io/en/post/cofiguration-of-my-mac/</link>
      <pubDate>Thu, 10 Jun 2021 13:38:00 +0800</pubDate>
      <guid>https://cauliyang.github.io/en/post/cofiguration-of-my-mac/</guid>
      <description>&lt;!-- &lt;div class=&#34;alert alert-note&#34;&gt;
  &lt;div&gt;
    All configuration or dot files are store in the Github, you can click Icon above to check that.
  &lt;/div&gt;
&lt;/div&gt;
 --&gt;
&lt;!-- @import &#34;[TOC]&#34; {cmd=&#34;toc&#34; depthFrom=1 depthTo=6 orderedList=false} --&gt;
&lt;!-- code_chunk_output --&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&#34;#1-applications-and-tools&#34;&gt;1. Applications and Tools&lt;/a&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&#34;#11-system-environment&#34;&gt;1.1 System Environment&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;&lt;a href=&#34;#2-editor&#34;&gt;2. Editor&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&#34;#3-application&#34;&gt;3. Application&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;!-- /code_chunk_output --&gt;
&lt;h2 id=&#34;1-applications-and-tools&#34;&gt;1. Applications and Tools&lt;/h2&gt;
&lt;p&gt;The first part will show the applications and tools I used for production, and every app or tool has a simple description. If you have an interest in a certain app, you can click the link I leave to get more information.&lt;/p&gt;
&lt;h3 id=&#34;11-system-environment&#34;&gt;1.1 System Environment&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&#34;https://brew.sh/&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;Homebrew&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Homebrew is an important tool for package manager in mac, which help you download and uninstall applications or tools efficiently. For detailed information, please click the link above to check that.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&#34;https://iterm2.com/&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;iterm2&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;iTerm2 is a replacement for Terminal and the successor to iTerm. It works on Macs with macOS 10.14 or newer. iTerm2 brings the terminal into the modern age with features you never knew you always wanted. Also, iterm2 has integrated &lt;a href=&#34;https://github.com/tmux/tmux/wiki&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;tmux&lt;/a&gt;, you can try to use &lt;strong&gt;tmux&lt;/strong&gt;, which is a terminal multiplexer. It lets you switch easily between several programs in one terminal, detach them (they keep running in the background), and reattach them to a different terminal.&lt;/p&gt;
&lt;p&gt;If you usually work on a remote server, it is a good idea to use &lt;strong&gt;tmux&lt;/strong&gt; in the remote server. That will help you keep the connection in case you lose control of the current shell in which you are working. There is a tip,  you can add &lt;code&gt;ssh -t user@hostname &amp;quot;tmux attach -t coding01 || tmux new -s coding01&amp;quot;&lt;/code&gt;. Then, you will connect the shell you have worked on last time when you log in to the remote server. If you never heard &lt;strong&gt;tmux&lt;/strong&gt;, you can click the link above to learn how to use it. I will also write a blog to show how I use &lt;strong&gt;tmux&lt;/strong&gt;, please check my blogs.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&#34;https://ohmyz.sh/&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;zsh&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Zsh is an alternative to Bash, and it has more advanced features. Oh My Zsh is a delightful, open-source, community-driven framework for managing your Zsh configuration. It comes bundled with thousands of helpful functions, helpers, plugins, themes, and a few things that make you shout. The screenshot of my terminal is shown in the figure below.
&lt;img src=&#34;https://cdn.jsdelivr.net/gh/cauliyang/blog-image@main//img/20210610155159.png&#34; alt=&#34;gos&#34;&gt;&lt;/p&gt;
&lt;p&gt;You can customize your own terminal, which will improve working efficiency. I use several useful plugins, you can learn how to config &lt;strong&gt;Oh my zsh&lt;/strong&gt; in &lt;font color=red size=5&gt;the blog&lt;/font&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&#34;https://vimawesome.com/&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;vim&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Vim is an editor used in the terminal, and I almost use it every day. Vim is shipped with Mac OS, and you also install other vim-extended editors such as &lt;a href=&#34;&#34;&gt;neovim&lt;/a&gt; and &lt;a href=&#34;&#34;&gt;spacevim&lt;/a&gt;. For me, I usually use the basic Vim with a relatively simple configuration. The &lt;a href=&#34;https://github.com/amix/vimrc&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;Github-repo&lt;/a&gt; store basic and awesome configuration. You can choose what you like to make your vim fancier.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&#34;https://git-scm.com/&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;git&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Git is a free and open-source distributed version control system designed to handle everything from small to very large projects with speed and efficiency. Also, Git a t tool used in the terminal to download and upload data or code to the [Github]. Similarly, Git is shipped with Mac OS so you may need to update that by &lt;code&gt;brew upgrade git&lt;/code&gt;. What is more, &lt;strong&gt;Oh my zsh&lt;/strong&gt; provides some alias and plugins, which help us to use git  easily.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&#34;https://docs.conda.io/en/latest/&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;conda&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Conda is a package, dependency, and environment management for any language such as &lt;em&gt;Python&lt;/em&gt;, &lt;em&gt;R&lt;/em&gt;, &lt;em&gt;Ruby&lt;/em&gt;, &lt;em&gt;C/C++&lt;/em&gt;, and more. Conda is an open-source package management system and environment management system that runs on Windows, macOS, and Linux. Conda quickly installs, runs, and updates packages and their dependencies. Conda easily creates, saves, loads, and switches between environments on your local computer. It was created for Python programs, but it can package and distribute software for any language.&lt;/p&gt;
&lt;p&gt;In addition, I recommend that you use &lt;a href=&#34;https://mamba.readthedocs.io/en/latest/&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;mamba&lt;/a&gt; to wrap &lt;strong&gt;Conda&lt;/strong&gt; to accelerate running speed.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&#34;https://tldr.sh/&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;tldr&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;The tldr pages are a community effort to simplify the beloved man pages with practical examples. You can get uasage clearly and quickly by using &lt;code&gt;tldr&lt;/code&gt; compared to &lt;code&gt;man&lt;/code&gt;. In mac, you can install that by &lt;code&gt;brew install tldr&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;&lt;img src=&#34;https://cdn.jsdelivr.net/gh/cauliyang/blog-image@main//img/20210610190712.png&#34; alt=&#34;&#34;&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&#34;https://www.geeksforgeeks.org/tree-command-unixlinux/&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;tree&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;tree&lt;/strong&gt; is a recursive directory listing program that produces a depth-indented listing of files. With no arguments, tree lists the files in the current directory. When directory arguments are given, tree lists all the files or directories found in the given directories each in turn. Upon completion of listing all files and directories found, tree returns the total number of files. You can install that by &lt;code&gt;brew install tree&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;&lt;img src=&#34;https://cdn.jsdelivr.net/gh/cauliyang/blog-image@main//img/20210610190826.png&#34; alt=&#34;&#34;&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&#34;https://tldr.sh/&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;tldr&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;The TLDR pages are a community effort to simplify the beloved man pages with practical examples. You can get usage clearly and quickly by using &lt;code&gt;tldr&lt;/code&gt; compared to &lt;code&gt;man&lt;/code&gt;. In mac, you can install that by &lt;code&gt;brew install tldr&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;&lt;img src=&#34;https://cdn.jsdelivr.net/gh/cauliyang/blog-image@main//img/20210610190712.png&#34; alt=&#34;&#34;&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&#34;https://www.geeksforgeeks.org/tree-command-unixlinux/&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;tree&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;tree&lt;/strong&gt; is a recursive directory listing program that produces a depth-indented listing of files. With no arguments, the tree lists the files in the current directory. When directory arguments are given, the tree lists all the files or directories found in the given directories each in turn. Upon completion of listing all files and directories found, the tree returns the total number of files. You can install that by &lt;code&gt;brew install tree&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;&lt;img src=&#34;https://cdn.jsdelivr.net/gh/cauliyang/blog-image@main//img/20210610190826.png&#34; alt=&#34;&#34;&gt;&lt;/p&gt;
&lt;h2 id=&#34;2-editor&#34;&gt;2. Editor&lt;/h2&gt;
&lt;p&gt;I will list the editors I used in different situations. If you have any other recommendations, please reach to me!&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&#34;https://code.visualstudio.com/&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;Vscode&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;em&gt;Vscode&lt;/em&gt; is an open-source editor, and I mainly use &lt;em&gt;Vscode&lt;/em&gt; to write &lt;em&gt;Markdown&lt;/em&gt;, &lt;em&gt;Latex&lt;/em&gt;, and &lt;em&gt;RST&lt;/em&gt;. Some simple modifications of the project for &lt;em&gt;Python&lt;/em&gt; and &lt;em&gt;C/C++&lt;/em&gt; will happen in &lt;em&gt;Vscode&lt;/em&gt;. There are lots of extensions to help you achieve your goals.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&#34;https://www.jetbrains.com/pycharm/&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;pycharm&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;em&gt;Pycharm&lt;/em&gt; is my main editor when I develop tools or applications in &lt;em&gt;Python&lt;/em&gt;. I use &lt;em&gt;EDU&lt;/em&gt; email to apply for the professional version.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&#34;https://www.jetbrains.com/clion/&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;CLion&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;em&gt;CLion&lt;/em&gt; is my &lt;em&gt;C/C++&lt;/em&gt; IDE, which includes many features like code generation, code quality assurance. I use &lt;em&gt;EDU&lt;/em&gt; email to apply for the professional version.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&#34;https://www.kite.com/&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;Kite&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;em&gt;Kite&lt;/em&gt; adds AI-powered code completions to your code editor, giving developers superpowers. I use &lt;em&gt;Kite&lt;/em&gt; to empower &lt;em&gt;Vscode&lt;/em&gt;, &lt;em&gt;Pycharm&lt;/em&gt;, &lt;em&gt;Vim&lt;/em&gt;, and &lt;em&gt;CLion&lt;/em&gt;.&lt;/p&gt;
&lt;h2 id=&#34;3-application&#34;&gt;3. Application&lt;/h2&gt;
&lt;p&gt;This part list variety of applications used for different goals. Every application has a simple description.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;Alfred 4&lt;br&gt;
A tool can give your different control and efficiency in mac.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Amphetamine&lt;/p&gt;
&lt;p&gt;Amphetamine can prevent your device from sleeping.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;CleanMyMac x
CleanMyMac is used to remove garbage and uninstall applications.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Default Folder x&lt;br&gt;
A tool can empower default finder.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Docker
Docker is a tool to create a safe environment for development or production.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Chrome&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;IINA&lt;/p&gt;
&lt;p&gt;A tool is used to play video.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Imagine&lt;/p&gt;
&lt;p&gt;A tool is used to compress images.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;IRightMouse Pro&lt;/p&gt;
&lt;p&gt;A tool is used to empower the right click of the mouse.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;LICEcap&lt;/p&gt;
&lt;p&gt;A tool is used to create gifs.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Magnet&lt;/p&gt;
&lt;p&gt;A tool is used to manage windows for different applications.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Office&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;MonitorControl&lt;/p&gt;
&lt;p&gt;A tool is used to manage brightness and sound for different monitors.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Monodraw&lt;/p&gt;
&lt;p&gt;A tool is used to design fancy ASCII strings.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Mountain Duck&lt;/p&gt;
&lt;p&gt;A tool is used to mount &lt;em&gt;Cloud&lt;/em&gt; or &lt;em&gt;Google Drive&lt;/em&gt; in local devices.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Paper&lt;/p&gt;
&lt;p&gt;A tool is used to change the Desktop background.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;PDF Expert&lt;/p&gt;
&lt;p&gt;A tool is used to edit and read PDF.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;PicGo&lt;/p&gt;
&lt;p&gt;A tool is used to upload images to &lt;em&gt;Web service&lt;/em&gt; like &lt;em&gt;Github&lt;/em&gt;. It is beneficial for writing blogs.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Pixel mator Pro&lt;/p&gt;
&lt;p&gt;A tool is used to modify images.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;R Studio&lt;/p&gt;
&lt;p&gt;An IDE for the R language.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;SnippetsLab&lt;/p&gt;
&lt;p&gt;A tool is used to keep code snippets.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Xcode&lt;/p&gt;
&lt;p&gt;A tool is used for development.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;zoom&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;igv&lt;/p&gt;
&lt;/li&gt;
&lt;/ul&gt;
</description>
    </item>
    
    <item>
      <title>Courses Presentations on Spring2021 </title>
      <link>https://cauliyang.github.io/en/talk/courses-presentations-on-spring2021/</link>
      <pubDate>Tue, 20 Apr 2021 10:04:04 +0800</pubDate>
      <guid>https://cauliyang.github.io/en/talk/courses-presentations-on-spring2021/</guid>
      <description>&lt;div class=&#34;alert alert-note&#34;&gt;
  &lt;div&gt;
    I begin to work for the two projects. Now I am choosing the topic of projects. Please wait for the update!
  &lt;/div&gt;
&lt;/div&gt;
</description>
    </item>
    
    <item>
      <title>Tools Comparision</title>
      <link>https://cauliyang.github.io/en/project/tools-comparision/</link>
      <pubDate>Sat, 17 Apr 2021 05:37:29 +0800</pubDate>
      <guid>https://cauliyang.github.io/en/project/tools-comparision/</guid>
      <description>&lt;details class=&#34;toc-inpage d-print-none  &#34; open&gt;
  &lt;summary class=&#34;font-weight-bold&#34;&gt;Table of Contents&lt;/summary&gt;
  &lt;nav id=&#34;TableOfContents&#34;&gt;
  &lt;ul&gt;
    &lt;li&gt;&lt;a href=&#34;#1-project-description&#34;&gt;1. Project Description&lt;/a&gt;&lt;/li&gt;
    &lt;li&gt;&lt;a href=&#34;#2-program-plan&#34;&gt;2. Program Plan&lt;/a&gt;
      &lt;ul&gt;
        &lt;li&gt;&lt;a href=&#34;#21--installation-of-software&#34;&gt;2.1  Installation of Software&lt;/a&gt;&lt;/li&gt;
        &lt;li&gt;&lt;a href=&#34;#22-create-test-data&#34;&gt;2.2 Create Test Data&lt;/a&gt;&lt;/li&gt;
        &lt;li&gt;&lt;a href=&#34;#23-test-tools-for-simple-data&#34;&gt;2.3 Test Tools for simple data&lt;/a&gt;&lt;/li&gt;
        &lt;li&gt;&lt;a href=&#34;#24-construct-snakemake-workflow&#34;&gt;2.4 Construct Snakemake Workflow&lt;/a&gt;&lt;/li&gt;
      &lt;/ul&gt;
    &lt;/li&gt;
  &lt;/ul&gt;
&lt;/nav&gt;
&lt;/details&gt;

&lt;h2 id=&#34;1-project-description&#34;&gt;1. Project Description&lt;/h2&gt;
&lt;p&gt;&lt;img src=&#34;https://cdn.jsdelivr.net/gh/cauliyang/blog-image@main//img/20210417053027.png&#34; alt=&#34;&#34;&gt;
The project aim to compare the performance for several kinds of tools used to dectect alternative splicing. The  tools are shown below:&lt;/p&gt;
&lt;h2 id=&#34;2-program-plan&#34;&gt;2. Program Plan&lt;/h2&gt;
&lt;h3 id=&#34;21--installation-of-software&#34;&gt;2.1  Installation of Software&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;&lt;input checked=&#34;&#34; disabled=&#34;&#34; type=&#34;checkbox&#34;&gt; &lt;a href=&#34;http://hollywood.mit.edu/burgelab/miso/&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;MISO&lt;/a&gt; | &lt;a href=&#34;https://miso.readthedocs.io/en/fastmiso/#using-miso-on-a-cluster&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;Documentation&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;input checked=&#34;&#34; disabled=&#34;&#34; type=&#34;checkbox&#34;&gt; &lt;a href=&#34;http://rnaseq-mats.sourceforge.net/index.html&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;rMATs&lt;/a&gt; | &lt;a href=&#34;https://github.com/Xinglab/rmats-turbo/blob/v4.1.1/README.md&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;Documentation&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;input checked=&#34;&#34; disabled=&#34;&#34; type=&#34;checkbox&#34;&gt; &lt;a href=&#34;https://majiq.biociphers.org/&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;MAJIQ&lt;/a&gt; | &lt;a href=&#34;https://biociphers.bitbucket.io/majiq/MAJIQ.html#builder&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;Documentation&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;input disabled=&#34;&#34; type=&#34;checkbox&#34;&gt; &lt;a href=&#34;https://davidaknowles.github.io/leafcutter/&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;LeafCutter&lt;/a&gt; | &lt;a href=&#34;https://davidaknowles.github.io/leafcutter/articles/Installation.html&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;Documentation&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;input checked=&#34;&#34; disabled=&#34;&#34; type=&#34;checkbox&#34;&gt; &lt;a href=&#34;https://github.com/ratschlab/spladder&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;SplAdder&lt;/a&gt; | &lt;a href=&#34;https://spladder.readthedocs.io/en/latest/general.html&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;Documentation&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;input checked=&#34;&#34; disabled=&#34;&#34; type=&#34;checkbox&#34;&gt; &lt;a href=&#34;https://github.com/qqwang-berkeley/JUM&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;Jum&lt;/a&gt; | &lt;a href=&#34;https://github.com/qqwang-berkeley/JUM/wiki/A-Hitchhiker%27s-guide-to-JUM-%28version-2.0.2-%29&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;Documentation&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;input checked=&#34;&#34; disabled=&#34;&#34; type=&#34;checkbox&#34;&gt; &lt;a href=&#34;https://github.com/timbitz/Whippet.jl&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;Whippet&lt;/a&gt; | &lt;a href=&#34;https://github.com/timbitz/Whippet.jl&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;Documentation&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;$\textbf{run_c.sh}$ records the usage and location of these tools.&lt;/p&gt;
&lt;h3 id=&#34;22-create-test-data&#34;&gt;2.2 Create Test Data&lt;/h3&gt;
&lt;p&gt;&lt;a href=&#34;https://github.com/biomedbigdata/ASimulatoR&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;ASimulatoR&lt;/a&gt; is used to create benchmark data to evaluate the performance of the tools mentioned above.&lt;/p&gt;
&lt;h3 id=&#34;23-test-tools-for-simple-data&#34;&gt;2.3 Test Tools for simple data&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;&lt;input disabled=&#34;&#34; type=&#34;checkbox&#34;&gt; &lt;a href=&#34;http://hollywood.mit.edu/burgelab/miso/&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;MISO&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;input disabled=&#34;&#34; type=&#34;checkbox&#34;&gt; &lt;a href=&#34;http://rnaseq-mats.sourceforge.net/index.html&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;rMATs&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;input disabled=&#34;&#34; type=&#34;checkbox&#34;&gt; &lt;a href=&#34;https://majiq.biociphers.org/&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;MAJIQ&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;input disabled=&#34;&#34; type=&#34;checkbox&#34;&gt; &lt;a href=&#34;https://davidaknowles.github.io/leafcutter/&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;LeafCutter&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;input disabled=&#34;&#34; type=&#34;checkbox&#34;&gt; &lt;a href=&#34;https://github.com/ratschlab/spladder&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;SplAdder&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;input disabled=&#34;&#34; type=&#34;checkbox&#34;&gt; &lt;a href=&#34;https://github.com/qqwang-berkeley/JUM&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;Jum&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;input disabled=&#34;&#34; type=&#34;checkbox&#34;&gt; &lt;a href=&#34;https://github.com/timbitz/Whippet.jl&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;Whippet&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&#34;24-construct-snakemake-workflow&#34;&gt;2.4 Construct Snakemake Workflow&lt;/h3&gt;
&lt;p&gt;&lt;a href=&#34;https://github.com/snakemake/snakemake-wrappers/blob/38ad23b0e4f58ce7dbd8d32612157f449ca02c62/docs/index.rst&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;&lt;img src=&#34;https://img.shields.io/badge/snakemake-%E2%89%A55.7.0-brightgreen.svg?style=flat-square&#34; alt=&#34;Snakemake&#34;&gt;&lt;/a&gt; is used to construct workflow.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;input disabled=&#34;&#34; type=&#34;checkbox&#34;&gt; Creat simulated data&lt;/li&gt;
&lt;li&gt;&lt;input disabled=&#34;&#34; type=&#34;checkbox&#34;&gt; Create Rna-align rule&lt;/li&gt;
&lt;li&gt;&lt;input disabled=&#34;&#34; type=&#34;checkbox&#34;&gt; Create Run-tools rules
&lt;ul&gt;
&lt;li&gt;&lt;input disabled=&#34;&#34; type=&#34;checkbox&#34;&gt; &lt;a href=&#34;http://hollywood.mit.edu/burgelab/miso/&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;MISO&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;input disabled=&#34;&#34; type=&#34;checkbox&#34;&gt; &lt;a href=&#34;http://rnaseq-mats.sourceforge.net/index.html&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;rMATs&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;input disabled=&#34;&#34; type=&#34;checkbox&#34;&gt; &lt;a href=&#34;https://majiq.biociphers.org/&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;MAJIQ&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;input disabled=&#34;&#34; type=&#34;checkbox&#34;&gt; &lt;a href=&#34;https://davidaknowles.github.io/leafcutter/&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;LeafCutter&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;input disabled=&#34;&#34; type=&#34;checkbox&#34;&gt; &lt;a href=&#34;https://github.com/ratschlab/spladder&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;SplAdder&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;input disabled=&#34;&#34; type=&#34;checkbox&#34;&gt; &lt;a href=&#34;https://github.com/qqwang-berkeley/JUM&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;Jum&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;input disabled=&#34;&#34; type=&#34;checkbox&#34;&gt; &lt;a href=&#34;https://github.com/timbitz/Whippet.jl&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;Whippet&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;&lt;input disabled=&#34;&#34; type=&#34;checkbox&#34;&gt; Create evaluation rules&lt;/li&gt;
&lt;li&gt;&lt;input disabled=&#34;&#34; type=&#34;checkbox&#34;&gt; Create report rules&lt;/li&gt;
&lt;/ul&gt;
</description>
    </item>
    
    <item>
      <title>Mining Cryptic Exon Using Deep Learning</title>
      <link>https://cauliyang.github.io/en/project/mining-cryptic-exon-using-deep-learning/</link>
      <pubDate>Mon, 05 Apr 2021 16:23:49 +0800</pubDate>
      <guid>https://cauliyang.github.io/en/project/mining-cryptic-exon-using-deep-learning/</guid>
      <description></description>
    </item>
    
    <item>
      <title>PCA by Python</title>
      <link>https://cauliyang.github.io/en/post/pca-by-python/</link>
      <pubDate>Sun, 04 Apr 2021 23:58:37 +0800</pubDate>
      <guid>https://cauliyang.github.io/en/post/pca-by-python/</guid>
      <description>&lt;!-- @import &#34;[TOC]&#34; {cmd=&#34;toc&#34; depthFrom=1 depthTo=6 orderedList=false} --&gt;
&lt;!-- code_chunk_output --&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&#34;#1-introduction&#34;&gt;1. Introduction&lt;/a&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&#34;#11-whats-pca&#34;&gt;1.1 What&amp;rsquo;s PCA?&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&#34;#12-algorithm&#34;&gt;1.2 Algorithm&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;&lt;a href=&#34;#2-pca-from-scratch&#34;&gt;2. PCA from scratch&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&#34;#3-pca-by--scikit-learn&#34;&gt;3. PCA by  scikit-learn&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&#34;#3-summary&#34;&gt;3. Summary&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;!-- /code_chunk_output --&gt;
&lt;h2 id=&#34;1-introduction&#34;&gt;1. Introduction&lt;/h2&gt;
&lt;h3 id=&#34;11-whats-pca&#34;&gt;1.1 What&amp;rsquo;s PCA?&lt;/h3&gt;
&lt;p&gt;When it comes to methods of reducing dimension, PCA that is an unsupervised linear transformation technique, must be not ignored. Moreover, if you want to know the subtle relationships among data set and reduce the computational complexity in downstream analysis, the PCA may be your best choice! Meanwhile, if you would like to present your data in a 2-dimension or 3-dimension coordinate system, and PCA would sweep your problems!&lt;/p&gt;
&lt;p&gt;What is reducing dimension? I will show you an example as follows:&lt;/p&gt;
&lt;p&gt;First, suppose you have a five-dimensional data set :&lt;/p&gt;
&lt;p&gt;| Id     | 1-d | 2-d | 3-d | 4-d | 5-d |
| &amp;mdash;&amp;mdash; | &amp;mdash; | &amp;mdash; | &amp;mdash; | &amp;mdash; | &amp;mdash; |  |
| data-1 | 1   | 2   | 3   | 4   | 5   |
| data-2 | 6   | 7   | 8   | 9   | 10  |
| ..     | ..  | ..  | ..  | ..  |&lt;/p&gt;
&lt;p&gt;Then, you could pick up PC1 and PC2 after PCA to reduce dimension for plotting:&lt;/p&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;Id&lt;/th&gt;
&lt;th&gt;PC1&lt;/th&gt;
&lt;th&gt;PC2&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;data-1&lt;/td&gt;
&lt;td&gt;0.3&lt;/td&gt;
&lt;td&gt;0.6&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;data-2&lt;/td&gt;
&lt;td&gt;0.1&lt;/td&gt;
&lt;td&gt;1.2&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;..&lt;/td&gt;
&lt;td&gt;..&lt;/td&gt;
&lt;td&gt;..&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;&lt;strong&gt;PC1&lt;/strong&gt; and &lt;strong&gt;PC2&lt;/strong&gt; are the result obtained through data is projection on the unit vectors, which enable result to have the most biggest variance(means its distribution is wide) and to be irrelevant(covariance = 0).&lt;/p&gt;
&lt;h3 id=&#34;12-algorithm&#34;&gt;1.2 Algorithm&lt;/h3&gt;
&lt;ol&gt;
&lt;li&gt;Normalize $d$ dimension raw data&lt;/li&gt;
&lt;li&gt;Creat the covariance matrix&lt;/li&gt;
&lt;li&gt;Calculate the eigenvalues of the covariance matrix and the corresponding eigenvectors&lt;/li&gt;
&lt;li&gt;The eigenvectors are sorted in the matrix according to the corresponding feature value, and the first k rows are formed into a matrix $W$. ($k&amp;laquo;d$)&lt;/li&gt;
&lt;li&gt;$Y = xW$ is the result after reducing dimension to k dimension&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;strong&gt;Note:&lt;/strong&gt; There are two prerequisites for conducting PCA&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Raw data has no NA&lt;/li&gt;
&lt;li&gt;The raw data should be normalized&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id=&#34;2-pca-from-scratch&#34;&gt;2. PCA from scratch&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;Importing necessary modules&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;import pandas as pd 
import numpy as np
import matplotlib.pyplot as plt 
from sklearn.model_selection import train_test_split
from sklearn.preprocessing import StandardScaler
&lt;/code&gt;&lt;/pre&gt;
&lt;ul&gt;
&lt;li&gt;Creating raw data&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;# get  data set 
df_wine = pd.read_csv(
    &#39;http://archive.ics.uci.edu/ml/machine-learning-databases/wine/wine.data&#39;,
    header=None,
    engine=&#39;python&#39;)
# check data
df_wine.head()
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;img src=&#34;https://cdn.jsdelivr.net/gh/cauliyang/blog-image@main//img/1572760322010.png&#34; alt=&#34;Test&#34;&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Creating train and test data set&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;# creat train and test data set 

X, y = df_wine.iloc[:, 1:], df_wine.iloc[:, 0]

x_train,x_test,y_train,y_test = \
    train_test_split(X,y,test_size = 0.3 , 
                    stratify= y,
                    random_state = 0 )
&lt;/code&gt;&lt;/pre&gt;
&lt;ul&gt;
&lt;li&gt;Standardizing the features&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;# create standard instance
sc = StandardScaler()
# standard data 
x_train_std = sc.fit_transform(x_train)
x_test_std = sc.fit_transform(x_test)
&lt;/code&gt;&lt;/pre&gt;
&lt;ul&gt;
&lt;li&gt;Creating the covariance matrix and Getting eigenvectors and eigenvalues&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;the calculation of the covariance matrix :
$$\sigma_{jk} =  \frac{1}{n} \sum^{n}_{i=1}\bigg(x_{j}^{(i)} - \mu_j\bigg)\bigg(x_{k}^{(i)} - \mu_k\bigg)$$
Then, using &lt;code&gt;numpy.cov&lt;/code&gt; and &lt;code&gt;numpy.linalg.eig&lt;/code&gt; to get the covariance matrix and eigenvectors respectively&lt;/p&gt;
&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;# calculate the covariance matrix 
cov_mat = np.cov(x_train_std.T)
# Getting eigenvectors and eigenvalues
eigen_vals, eigen_vecs = np.linalg.eig(cov_mat)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;NOTE:&lt;/strong&gt; there are 13 eigenvectors totally, the number of eigenvalues might be not as same as the number of features sometimes.&lt;/p&gt;
&lt;p&gt;Firstly, plotting the Variance interpretation ratio, which is obtained through eigenvalue $\lambda_j$ divided by the sum of all the eigenvalues:
$$ \frac{\lambda_j}{\sum^d_{j=1}\lambda_j}$$&lt;/p&gt;
&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;# get sum of all the eigenvalues
tot = sum(eigen_vals)
# get variance interpretation ratio
var_exp = [(i / tot) for i in sorted(eigen_vals, reverse=True)]
cum_var_exp = np.cumsum(var_exp)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Besides, plotting the result to get in-depth understanding:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;plt.figure() # creat plot
# creat bar plot
plt.bar(
    range(1, 14),
    var_exp,
    alpha=0.5,
    label=&#39;individual explained variance&#39;,
)
# creat step plot 
plt.step(range(1, 14),
         cum_var_exp,
         where=&#39;mid&#39;,
         label=&#39;cumulative explained variance&#39;)
# add label 
plt.ylabel(&#39;Explained variance ratio&#39;)
plt.xlabel(&#39;Principal component index&#39;)
# add legend
plt.legend(loc=&#39;best&#39;)
# save picture
plt.savefig(&#39;pca_index.png&#39;, format=&#39;png&#39;, bbox_inches=&#39;tight&#39;, dpi=300)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;img src=&#34;https://cdn.jsdelivr.net/gh/cauliyang/blog-image@main//img/1572764173795.png&#34; alt=&#34;&#34;&gt;
We can conclude that &lt;strong&gt;PC1&lt;/strong&gt; only takes account for about 40%. Furthermore, the sum of &lt;strong&gt;PC1&lt;/strong&gt; and &lt;strong&gt;PC2&lt;/strong&gt; have 60% variance.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Selecting the first &lt;strong&gt;k&lt;/strong&gt; values to form matrix $W$&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;# integrate eigenvalues  and eigenvectors 
eigen_paris = [(np.abs(eigen_vals[i]), eigen_vecs[:, i])
               for i in range(len(eigen_vals))]
# sort according to eigenvalues 
eigen_paris.sort(key=lambda x: x[0], reverse=True)
# pick up the first 2 eigenvalues 
w = np.hstack(
    [eigen_paris[0][1][:, np.newaxis], eigen_paris[1][1][:, np.newaxis]])
# check matrix x 
w
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;img src=&#34;https://cdn.jsdelivr.net/gh/cauliyang/blog-image@main//img/1572766213478.png&#34; alt=&#34;&#34;&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Transforming raw data&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;# reduce dimension 
x_train_pca = x_train_std.dot(w)
# check resulted data 
x_train_pca.shape
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;code&gt;(124, 2)&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;Then plotting the result and putting the label in terms of original info, but keeping in mind PCA is unsupervised learning skill without labels&lt;/p&gt;
&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;# init colors and markers 
colors = [&#39;r&#39;, &#39;b&#39;, &#39;g&#39;]
markers = [&#39;s&#39;, &#39;x&#39;, &#39;o&#39;]
# plot scatter
for l, c, m in zip(np.unique(y_train), colors, markers):
    plt.scatter(x_train_pca[y_train == l, 0],
                x_train_pca[y_train == l, 1],
                c=c,
                label=1,
                marker=m)
# add label and legend 
plt.xlabel(&#39;PC 1&#39;)
plt.ylabel(&#39;PC 2&#39;)
plt.legend(loc=&#39;lower left&#39;)
plt.savefig(&#39;distribution.png&#39;, format=&#39;png&#39;, bbox_inches=&#39;tight&#39;, dpi=300)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;img src=&#34;https://cdn.jsdelivr.net/gh/cauliyang/blog-image@main//img/1572766761786.png&#34; alt=&#34;&#34;&gt;&lt;/p&gt;
&lt;h2 id=&#34;3-pca-by--scikit-learn&#34;&gt;3. PCA by  scikit-learn&lt;/h2&gt;
&lt;p&gt;we can  conduct PCA easily by &lt;strong&gt;sklearn&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Importing modules&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;from sklearn.decomposition import PCA
from matplotlib.colors import ListedColormap
from sklearn.linear_model import LogisticRegression
&lt;/code&gt;&lt;/pre&gt;
&lt;ul&gt;
&lt;li&gt;Defining  function of plot_decision_region&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;
def plot_dicision_regions(X, y, classifier, resolution=0.02):
    # init markers and colors 
    markers = (&#39;s&#39;, &#39;x&#39;, &#39;o&#39;, &#39;^&#39;, &#39;v&#39;)
    colors = (&#39;red&#39;, &#39;blue&#39;, &#39;lightgreen&#39;, &#39;gray&#39;, &#39;cyan&#39;)
    cmap = ListedColormap(colors[:len(np.unique(y))])
    # creat info for plot region
    x1_min, x1_max = X[:, 0].min() - 1, X[:, 0].max() + 1
    x2_min, x2_max = X[:, 1].min() - 1, X[:, 1].max() + 1
    xx1, xx2 = np.meshgrid(np.arange(x1_min, x1_max, resolution),
                           np.arange(x2_min, x2_max, resolution))
    # test classifier&#39;s accurate 
    z = classifier.predict(np.array([xx1.ravel(), xx2.ravel()]).T)
    z = z.reshape(xx1.shape)
    # plot desicion region 
    plt.contourf(xx1, xx2, z, alpha=0.4, cmap=cmap)
    # set x,y length
    plt.xlim(xx1.min(), xx1.max())
    plt.ylim(xx2.min(), xx2.max())
    # plot result 
    for idx, cl in enumerate(np.unique(y)):
        plt.scatter(
            x=X[y == cl, 0],
            y=X[y == cl, 1],
            alpha=0.6,
            color=cmap(idx),
            edgecolor=&#39;black&#39;,
            marker=markers[idx],
            label=cl,
        )
&lt;/code&gt;&lt;/pre&gt;
&lt;ul&gt;
&lt;li&gt;PCA by sklearn&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;# creat pca instance 
pca = PCA(n_components = 2 )
# creat classifier instance 
lr = LogisticRegression()
# reduce dimension for  data set 
x_train_pca = pca.fit_transform(x_train_std)
x_test_pca  = pca.transform(x_test_std)
# classify x_train_pca 
lr.fit(x_train_pca,y_train)
# plot dicision region
plot_dicision_regions(x_train_pca,y_train,classifier=lr)
# add info
plt.xlabel(&#39;PC 1&#39;)
plt.ylabel(&#39;PC 2&#39;)
plt.legend(loc=&#39;lower left&#39;)
plt.show()
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;img src=&#34;https://cdn.jsdelivr.net/gh/cauliyang/blog-image@main//img/1572767868782.png&#34; alt=&#34;&#34;&gt;
We can see that the classifier&amp;rsquo;s accurate is excellent according to actual labels&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;TIPS&lt;/strong&gt;:&lt;/p&gt;
&lt;p&gt;You can set &lt;code&gt;n_components = None&lt;/code&gt;, and the result would retain all principle components. Moreover, you could call &lt;code&gt;explained_variance_ration_&lt;/code&gt; to use variance explanation ratio.&lt;/p&gt;
&lt;h2 id=&#34;3-summary&#34;&gt;3. Summary&lt;/h2&gt;
&lt;p&gt;All the above are the main content, welcome everybody communicates with me! 🤠&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Reference book :&lt;/strong&gt; &lt;a href=&#34;https://www.amazon.com/dp/B0742K7HYF/ref=sr_1_1?__mk_zh_CN=%E4%BA%9A%E9%A9%AC%E9%80%8A%E7%BD%91%E7%AB%99&amp;amp;crid=27TEKOK8R4TOR&amp;amp;keywords=python&amp;#43;machine&amp;#43;learning&amp;#43;sebastian&amp;#43;raschka&amp;amp;qid=1572770147&amp;amp;s=digital-text&amp;amp;sprefix=python&amp;#43;machine&amp;#43;&amp;#43;learning&amp;#43;seb%2Cdigital-text%2C389&amp;amp;sr=1-1&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;Python machine learning&lt;/a&gt;&lt;/p&gt;
</description>
    </item>
    
    <item>
      <title>Nei Saitou Neighbor Joining</title>
      <link>https://cauliyang.github.io/en/post/nei-saitou-neighbor-joining/</link>
      <pubDate>Sat, 03 Apr 2021 11:15:00 +0800</pubDate>
      <guid>https://cauliyang.github.io/en/post/nei-saitou-neighbor-joining/</guid>
      <description>&lt;details class=&#34;toc-inpage d-print-none  &#34; open&gt;
  &lt;summary class=&#34;font-weight-bold&#34;&gt;Table of Contents&lt;/summary&gt;
  &lt;nav id=&#34;TableOfContents&#34;&gt;
  &lt;ul&gt;
    &lt;li&gt;&lt;a href=&#34;#1-background&#34;&gt;1. Background&lt;/a&gt;&lt;/li&gt;
    &lt;li&gt;&lt;a href=&#34;#2-neighbor-joining-algorithm&#34;&gt;2. Neighbor Joining Algorithm&lt;/a&gt;&lt;/li&gt;
    &lt;li&gt;&lt;a href=&#34;#3-implementation&#34;&gt;3. Implementation&lt;/a&gt;&lt;/li&gt;
  &lt;/ul&gt;
&lt;/nav&gt;
&lt;/details&gt;

&lt;h2 id=&#34;1-background&#34;&gt;1. Background&lt;/h2&gt;
&lt;p&gt;Before diving into code, the description of NJ algorithm can be found in &lt;img src=&#34;https://cdn.jsdelivr.net/gh/cauliyang/blog-image@main//img/1605172209524.png&#34; alt=&#34;This Link&#34;&gt;, where first column indicates parent node, and second column is its children node, the last column is the value of edge.&lt;/p&gt;
&lt;h2 id=&#34;2-neighbor-joining-algorithm&#34;&gt;2. Neighbor Joining Algorithm&lt;/h2&gt;
&lt;p&gt;The Neighbor-joining Algorithm Given a distance matrix d compute an unrooted tree topology complete with edge lengths that tries to preserve the additive property: $d_ {i,m} + d_{j,m} − d_{i,j} = 2d_{k,m}$ where $k$ is the ﬁrst node on both routes from $i$ and $j$ to $m$.&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;Let the set of clusters be called $L$ and initially $i → C_i; ∀i$ that is $| C_i | = 1$ and $L = C_1 , C_2 , . . . C_N$.&lt;/li&gt;
&lt;li&gt;$d_{i,j}$ is the distance from the initial distance matrix.&lt;/li&gt;
&lt;li&gt;Compute “normalized distance matrix” $D_{i,j}$ for all $i, j$ such that
$$D_{i,j} = d_{i,j} − (r_i + r_j ) \ where\ r_i = \frac{1}{|L|-2} \sum_{z \in L} d_{i,z}$$
This subtracts the average distance to all other nodes than the pair involved. **Note: this is not where we use the distance identity.**&lt;/li&gt;
&lt;li&gt;Use normalized distance to ﬁnd $(i, j) = argmin D_{i,j}; C_i,C_j \in L$&lt;/li&gt;
&lt;li&gt;Merge $C_i ∪ C_j → C_k$ where $k$ is a new cluster number.&lt;/li&gt;
&lt;li&gt;Mark old clusters as used so that eﬀectively: $L ← L − C_i − C_j$&lt;/li&gt;
&lt;li&gt;Compute a new unnormalized distance matrix including the new cluster $k$ and excluding $i, j$.
$$d_{k,z} = d_{z,k} = (d_{i,z} + d_{j,,z} − d_{i,j} ) for all z ∈ L 2$$
This uses the additivity of the distances to compute the distance to the new cluster from each other node.&lt;/li&gt;
&lt;li&gt;Compute the length of the edges from $k$ to $i$ and $j$. Even though $C_k$has assumed the role of both $C_i$ and $C_j$  you still need the edge length to $i$ and $j$ from $k$ in order to “draw” the tree.
$$edge_{i,k} = (d_{i,j} + r_i − r_j)$$
$$edge_{j,k} = (d_{i,j} + r_j − r_i)$$&lt;/li&gt;
&lt;li&gt;Deﬁne height $h_k = d_{i,j} /2$ where $h_k$ is the height of node that is the ancestor to all in $C_k$ . When drawing the tree $h_k$ is the height above the baseline (where all the leaves are).&lt;/li&gt;
&lt;li&gt;$L ← L ∪ C_k$&lt;/li&gt;
&lt;li&gt;While there is more than two clusters left go to step 3&lt;/li&gt;
&lt;li&gt;Finally, join the remaining two clusters with:
$$edge_{j,k} = d_{i,j} $$&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;strong&gt;Implementation Notes Consider this part of the computation:&lt;/strong&gt;
$$D_{i,j} = d_{i,j} − (r_i + r_j ) \ where \ r_i =  \frac{1}{|L|-2} \sum_{z \in L}d_{i,z}$$
The values of r_z can be computed once each time we want to compute matrix $D$. This saves a vast amount of time. Furthermore, since $D_{i,j}$ is only used to ﬁnd the argmin of $D_{i,j}$ we actually don’t have to save array $D$; we only need to ﬁnd the argmin of it. So ﬁrst compute all the r and then combine the argmin step with the computation of $D_{i,j}$&lt;/p&gt;
&lt;h2 id=&#34;3-implementation&#34;&gt;3. Implementation&lt;/h2&gt;
&lt;p&gt;I write code contained comments, and it is about 1000 lines that consumes me two whole days. Now let me show my code with rich comments. If you have any questions or recommendation, I am very glad to communicate with you! Please feel free to reach me.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;# -*- coding: UTF-8 -*-
# Time 20201109
&#39;&#39;&#39;
This code integrate bootstrap, geting edge_file and tree_file

USAGE: python -i fa_file -d out_distance_matrix -e out_edge -t out_tree -b number_bootstrap
&#39;&#39;&#39;
# import needed library
import argparse
import logging
import random
import time
from collections import defaultdict, namedtuple

import pandas as pd


class Timer:
    &amp;quot;&amp;quot;&amp;quot;[construct  Timer to show working time of tasks]
    &amp;quot;&amp;quot;&amp;quot;
    def __init__(self, func=time.perf_counter):
        &amp;quot;&amp;quot;&amp;quot;[init values]

        Args:
            func : Defaults to time.perf_counter.
        &amp;quot;&amp;quot;&amp;quot;
        self.elapsed = 0.0

        self._func = func

        self._start = None

    def start(self):
        &amp;quot;&amp;quot;&amp;quot;[start a task]

        Raises:
            RuntimeError:[if task has started then raise error]
        &amp;quot;&amp;quot;&amp;quot;

        if self._start is not None:
            raise RuntimeError(&#39;Already started&#39;)

        self._start = self._func()

    def stop(self):
        &amp;quot;&amp;quot;&amp;quot;[end a task]

        Raises:
            RuntimeError[if task has not started then raise error]
        &amp;quot;&amp;quot;&amp;quot;
        if self._start is None:
            raise RuntimeError(&#39;Not started&#39;)

        end = self._func()

        self.elapsed += end - self._start

        self._start = None

    def reset(self):
        &amp;quot;&amp;quot;&amp;quot;[reset the working time]
        &amp;quot;&amp;quot;&amp;quot;
        self.elapsed = 0

    def running(self):
        &amp;quot;&amp;quot;&amp;quot;[check if task is running]

        Returns:
            bool
        &amp;quot;&amp;quot;&amp;quot;

        return self._start is not None

    def __enter__(self):
        &amp;quot;&amp;quot;&amp;quot;[function used to address &#39;with text&#39;]
        &amp;quot;&amp;quot;&amp;quot;

        self.start()

        return self

    def __exit__(self, *args):
        &amp;quot;&amp;quot;&amp;quot;[function used to address &#39;with text&#39;]
        &amp;quot;&amp;quot;&amp;quot;

        self.stop()


class TreeNode:
    &amp;quot;&amp;quot;&amp;quot;[class of tree node]

    &amp;quot;&amp;quot;&amp;quot;
    def __init__(self, key=None):
        &amp;quot;&amp;quot;&amp;quot;[init values of tree node]

        Args:
            key ([str, float], optional): [node_label, edge]. Defaults to None.

            self.index: node_index
        &amp;quot;&amp;quot;&amp;quot;
        self.left, self.right = None, None

        self.key = key

        self.parent, self.index = None, None

        self.LIST = []

    def insert_left_children(self, left_object):
        &amp;quot;&amp;quot;&amp;quot;[insert left children]

        Args:
            left_object ([str, float]): [node_label, edge]
        &amp;quot;&amp;quot;&amp;quot;
        # create a new node
        temp = TreeNode(left_object)
        # if left children exist
        if self.left:

            temp.left = self.left

            self.left = temp
        # if left children does not exist
        else:
            self.left = temp

    def insert_right_children(self, right_object):
        &amp;quot;&amp;quot;&amp;quot;[insert right children]

        Args:
            right_object ([str, float]): [node_label, edge]
        &amp;quot;&amp;quot;&amp;quot;
        # create a new node
        temp = TreeNode(right_object)
        # if right childern exist
        if self.right:

            temp.right = self.right

            self.right = temp
        # if right children does not exist
        else:
            self.right = temp

    def isRoot(self):
        &amp;quot;&amp;quot;&amp;quot;[check if the node is Root node]

        Returns:
            [bool]: [if node is Root return True]
        &amp;quot;&amp;quot;&amp;quot;

        handle = False

        if self.key[0] == &#39;root&#39;:

            handle = True

        return handle

    def isLeaf(self):
        &amp;quot;&amp;quot;&amp;quot;[check if the node is Leaf node]

        Returns:
            [bool]: [if the node is Leaf return True]
        &amp;quot;&amp;quot;&amp;quot;

        handle = False

        if not self.left and not self.right:

            handle = True

        return handle

    def catch_leaf(self):
        &amp;quot;&amp;quot;&amp;quot;[show the partion list of leafs below the node, and mainly used to debug]

        Returns:
            [list]: [a list contained all leaf below the node]
        &amp;quot;&amp;quot;&amp;quot;

        LIST = []

        # check if the node is leaf
        if self.isLeaf():

            LIST.append(self.index)

        if self.left:

            LIST.extend(self.left.catch_leaf())

        if self.right:

            LIST.extend(self.right.catch_leaf())

        return LIST

    def postorder(self):
        &amp;quot;&amp;quot;&amp;quot;[tree traversal with postorder, and mainly used to debug]
        &amp;quot;&amp;quot;&amp;quot;
        if self.left:

            self.left.postorder()

        if self.right:

            self.right.postorder()

        print(self.key)


def get_parser() -&amp;gt; dict:
    &amp;quot;&amp;quot;&amp;quot;[function used to parse parameters from command line and set log]

    Returns:
        [dict]: [return a dict stored parameters]
    &amp;quot;&amp;quot;&amp;quot;
    # set logging
    logging.basicConfig(level=logging.DEBUG,
                        format=&#39;%(levelname)s:%(asctime)s:%(message)s&#39;)

    # set parser
    parser = argparse.ArgumentParser(
        prog=&#39;PROG&#39;,
        description=&#39;Program designed to condcut NJ &#39;,
        formatter_class=argparse.MetavarTypeHelpFormatter)

    # add parameter for input of fa file
    parser.add_argument(&#39;-i&#39;,
                        &#39;--input&#39;,
                        help=&#39;The input file (fasta format)&#39;,
                        required=True,
                        type=str)

    # add parameter for outout of edge file
    parser.add_argument(
        &#39;-e&#39;,
        &#39;--edge&#39;,
        help=&#39;The output file of edges matrix. Default: edges.txt&#39;,
        default=&#39;edges.txt&#39;,
        type=str)

    # add parameter for output of tree file
    parser.add_argument(
        &#39;-t&#39;,
        &#39;--tree&#39;,
        help=&#39;The output file of newick tree. Default: tree.txt&#39;,
        default=&#39;tree.txt&#39;,
        type=str)

    # add parameter for output of distance matrix
    parser.add_argument(
        &#39;-d&#39;,
        &#39;--distance&#39;,
        help=
        &#39;The output file of distance matrix. Default: genetic_distance.txt&#39;,
        default=&#39;genetic_distance.txt&#39;,
        type=str)

    # add parametr for number of boostrap; Default: not conduct bootstrap
    parser.add_argument(
        &#39;-b&#39;,
        &#39;--bootstrap&#39;,
        help=&amp;quot;The number of boostrap. Result will be stored in &#39;bootstrap.txt&#39;&amp;quot;,
        default=None,
        type=int)

    # get parameters
    args = parser.parse_args()

    return args


##############################################
#CELL FOR GETING GENETIC DISTANCE MATRIX
##############################################


def read_input(input: str) -&amp;gt; dict:
    &amp;quot;&amp;quot;&amp;quot;[read input of fa file and return a dict whose value is nametuple stored information]

    Args:
        input (str): [fasta file]

    Returns:
        dict: [seq_label: (seq,index )]
    &amp;quot;&amp;quot;&amp;quot;
    #  create namedtuple to store information
    seqs, seq = {}, namedtuple(&#39;info&#39;, [&#39;seq&#39;, &#39;index&#39;])

    # open file as a list
    content = open(input).readlines()

    # init index of sequence
    read_index = 1

    # read seq into the dict
    for index, line in enumerate(content):
        # check header
        if line.startswith(&#39;&amp;gt;&#39;):

            seqs[line.strip(&#39;&amp;gt;\n&#39;)] = seq(content[index + 1].strip(),
                                          read_index)
            read_index += 1

    return seqs


def genetic_distance(seq1: str, seq2: str) -&amp;gt; float:
    &amp;quot;&amp;quot;&amp;quot;[calculate genetic distance of seq1 and seqw]

    Args:
        seq1 (str): [sequences of seq1]
        seq2 (str): [sequences of seq2]

    Returns:
        float: [genetic distance]
    &amp;quot;&amp;quot;&amp;quot;
    # get length of sequences
    length = len(seq1)

    # init mismatch
    mismatch = 0

    # begin to calculate
    for s1, s2 in zip(seq1, seq2):

        if s1 != s2:

            mismatch += 1

    return mismatch / length


def fetch_distance(seq1_index: str, seq2_index: str,
                   matrix: dict) -&amp;gt; float or None:
    &amp;quot;&amp;quot;&amp;quot;[fetch distance from distance matrix in case comupute pair distance repeatly]

    Args:
        seq1_index (str): [seq1 label]
        seq2_index (str): [seq2 label]
        matrix (dict): [store distances that is calculated]

    Returns:
        float or None: [if distance has calculated return value otherwise None]
    &amp;quot;&amp;quot;&amp;quot;
    # init value
    distance = None

    # check if the distance is already calculated
    if (seq1_index, seq2_index) in matrix:

        distance = matrix[(seq1_index, seq2_index)]

    elif (seq2_index, seq1_index) in matrix:

        distance = matrix[(seq2_index, seq1_index)]

    return distance


def get_distance_matrix(seqs: dict, out: str) -&amp;gt; dict:
    &amp;quot;&amp;quot;&amp;quot;[write distance matrix to file]

    Args:
        seqs (dict): [store infomation of seqs like {seq_label: (seq:, index:)}]
        out (str): [the name of genetic distance file]

    Returns:
        dict: [{(seq1_label,seq2_label):genetic_distance}]
    &amp;quot;&amp;quot;&amp;quot;
    # open new file
    out_put = open(out, &#39;w&#39;)

    # init dict
    distance_map = {}

    # write header
    header = &#39;\t&#39;.join(seqs.keys())
    out_put.write(f&#39;\t{header}\n&#39;)

    # begin to calculate distance and write to file
    for seq1_label, seq1_info in seqs.items():
        # init list to store distance of every line
        line = []

        for seq2_label, seq2_info in seqs.items():
            # check if the distance is already computed
            distance = fetch_distance(seq1_label, seq2_label, distance_map)

            if not distance:
                # if not exist then calculate it and store
                distance = genetic_distance(seq1_info.seq, seq2_info.seq)

                distance_map[(seq1_label, seq2_label)] = distance

            line.append(distance)
        # write every line
        line = &#39;\t&#39;.join([str(element) for element in line])
        out_put.write(f&#39;{seq1_label}\t{line}\n&#39;)

    out_put.close()

    return distance_map


##############################################
#                            NJ ALGORITHM
##############################################


def cacl_sum_distance(taxa: str, leaf_set: set,
                      distance_matrix: dict) -&amp;gt; float:
    &amp;quot;&amp;quot;&amp;quot;[calculate sum distance of node away from other nodes]

    Args:
        taxa ([str]): [label for one node]
        leaf_set ([set]): [a set store current nodes needed to be merged]
        distance_matrix ([dict]): [{(seq1_label,seq2_label):genetic_distance}]

    Returns:
        [float]: [sum distance of node away from others]
    &amp;quot;&amp;quot;&amp;quot;
    # init value
    result = 0
    # calculate sum distance
    for other_taxa in leaf_set - set([taxa]):
        # fectch genetic distance
        result += fetch_distance(taxa, other_taxa, distance_matrix)

    return result


def find_qmin(leaf_set: set, distance_matrix: dict) -&amp;gt; dict:
    &amp;quot;&amp;quot;&amp;quot;[ calculate min q_value and get information]

    Args:
        leaf_set ([set]): [current nodes needed to be merged]
        distance_matrix ([dict]): [dict stored genetic distance of current nodes]

    Returns:
        [dict]:  [value, combination,taxa1_sum, taxa2_sum]
    &amp;quot;&amp;quot;&amp;quot;
    # init dict
    qmin = {
        &#39;combination&#39;: None,  # combination of node for merging
        &#39;q_value&#39;: None,  # min Q value
        &#39;taxa1_sum&#39;: None,  # sum distance of node away from others
        &#39;taxa2_sum&#39;: None
    }

    # calculate  Q value every combinatin of nodes
    for combination, distance in distance_matrix.items():

        taxa1, taxa2 = combination
        # calculate sum distance of node away from others
        taxa1_sum, taxa2_sum = cacl_sum_distance(
            taxa1, leaf_set,
            distance_matrix), cacl_sum_distance(taxa2, leaf_set,
                                                distance_matrix)
        # get Q value
        value = (len(leaf_set) - 2) * distance - taxa1_sum - taxa2_sum
        # compare Q value
        if not qmin[&#39;q_value&#39;]:
            # if Q value does not exist then store related information
            qmin[&#39;q_value&#39;], qmin[&#39;combination&#39;], qmin[&#39;taxa1_sum&#39;], qmin[
                &#39;taxa2_sum&#39;] = value, combination, taxa1_sum, taxa2_sum
        # if Q value exist then compare to choose minimun value
        else:
            if value &amp;lt; qmin[&#39;q_value&#39;]:

                qmin[&#39;q_value&#39;], qmin[&#39;combination&#39;], qmin[&#39;taxa1_sum&#39;], qmin[
                    &#39;taxa2_sum&#39;] = value, combination, taxa1_sum, taxa2_sum

    return qmin


def help_nj(leaf_set: set, distance_matrix: dict):
    &amp;quot;&amp;quot;&amp;quot;[helper function to conduct neighbor joining]

    Args:
        leaf_set ([set]): [current nodes needed to be merged]
        distance_matrix ([dict]): [store pair distance of current nodes]

    Returns:
        TREEDICT: a dict stored node that has been merged in binary tree format
        final_leaf_set: final twos node after finshing NJ
        final_matrix:  genetic distances of final two nodes
    &amp;quot;&amp;quot;&amp;quot;
    # init dict to store nodes that has been merged in format of Binary Tree
    TREEDICT = {}

    def neighbor_joining(leaf_set: set, distance_matrix: dict):
        &amp;quot;&amp;quot;&amp;quot;[implementation of neighbors joining]

        Args:
            leaf_set ([set]): [current nodes needed to be merged]
            distance_matrix ([dict]): [store genetic distance of current nodes]
        &amp;quot;&amp;quot;&amp;quot;
        # get nonlocal value
        nonlocal TREEDICT

        # the rule to end the recursion
        if len(leaf_set) &amp;lt; 3:

            return (leaf_set, distance_matrix)
        # enter recursion
        else:
            # find min Q value
            qmin_info = find_qmin(leaf_set, distance_matrix)
            # get nodes needed to be merged
            taxa1, taxa2 = qmin_info[&#39;combination&#39;]
            # edge from taxa1 to new node
            edge_taxa1_node = 0.5 * distance_matrix[
                qmin_info[&#39;combination&#39;]] + (qmin_info[&#39;taxa1_sum&#39;] -
                                             qmin_info[&#39;taxa2_sum&#39;]) / (
                                                 2 * (len(leaf_set) - 2))
            # edge from taxa1 to new node
            edge_taxa2_node = distance_matrix[
                qmin_info[&#39;combination&#39;]] - edge_taxa1_node

            # init dict to update genetic matrix
            matrix = {}
            # create label for new node
            new_node = f&#39;{taxa1}_{taxa2}&#39;
            # calculate distance from new node to other nodes
            for node in leaf_set - set(qmin_info[&#39;combination&#39;]):
                # store in new genetic matrix with information of old genetic matrix
                matrix[(
                    new_node,
                    node)] = (fetch_distance(taxa1, node, distance_matrix) +
                              fetch_distance(taxa2, node, distance_matrix) -
                              distance_matrix[(taxa1, taxa2)]) * 0.5

            # store genetic distances of other nodes in new genetic matrix
            for combination, distance in distance_matrix.items():

                if (taxa1 not in combination) and (taxa2 not in combination):

                    matrix[combination] = distance

            # store phylogenetic nodes that has been merged in Binary Tree format
            # creat a tree node that linked two merged nodes in Binary Tree

            # if one of merged nodes has been in Binary Tree then add edges
            # taxa1 is always regarded as left children
            if taxa1 in TREEDICT:

                left = TREEDICT.pop(taxa1)
                left.key[1] = edge_taxa1_node

            # if one of merged nodes is not stored in Binary Tree
            else:
                # create a Tree node and store its label and edge
                left = TreeNode([taxa1, edge_taxa1_node])

            # taxa2 is same with taxa1
            # taxa2 is always regarded as right children
            if taxa2 in TREEDICT:

                right = TREEDICT.pop(taxa2)
                right.key[1] = edge_taxa2_node

            else:

                right = TreeNode([taxa2, edge_taxa2_node])

            # create a new_node linked two merged nodes in Binary Tree
            # the new_node has no edge
            internal_node = TreeNode([new_node, None])
            # add paraent information for two merged nodes
            left.parent, right.parent = internal_node, internal_node
            # link two merged nodes as children in Binary Tree
            internal_node.left, internal_node.right = left, right
            # add new node to dict
            TREEDICT[new_node] = internal_node
            # update genetic distance matrix
            distance_matrix = matrix
            # update current phylogenetic nodes needed to be merged
            leaf_set -= set(qmin_info[&#39;combination&#39;])
            leaf_set.add(new_node)

            return neighbor_joining(leaf_set, distance_matrix)

    # begin to compute until two phylogenetic nodes left
    final_leaf_set, final_matrix = neighbor_joining(leaf_set, distance_matrix)

    return TREEDICT, final_leaf_set, final_matrix


##############################################
#               PREORDER TREE AND ADD INDEX
##############################################


def my_tree(TREEDICT: dict, final_distance_matrix: dict, seqs: dict):
    &amp;quot;&amp;quot;&amp;quot;   function to create Binary tree but it has fake root
            because phylogenetic node has three childern
            hence create a fake node as root to get a Binary Tree
            all following behaivors related Binar Tree usually consider this situation

            for binary_tree.key:

            key[0] -&amp;gt; label ; key[1] -&amp;gt; edge

    Args:
        TREEDICT ([dict]): [store information of binary tree ]
        final_distance_matrix ([dict]): [distance of final two nodes]
        seqs ([dict]): [store information of seqs]

    Returns:
        [binary tree]: [binary tree with fake root]
    &amp;quot;&amp;quot;&amp;quot;
    # &#39;merged node&#39; means nodes have been changed as binary tree
    # init value to find if node is &#39;merged node&#39;  in final two nodes after NJ
    last_node_label = None
    last_node_edges = None

    for index, value in final_distance_matrix.items():

        # get edge
        last_node_edges = value

        # check if node is &#39;merged node&#39;
        for label in index:
            # if not
            if label in seqs:
                # get label
                last_node_label = label

    # create a fake root with label &#39;root&#39;
    fake_root = TreeNode((&#39;root&#39;, &#39; &#39;))
    # get node label of final two nodes after NJ
    current_node_index = list(TREEDICT.keys())
    # if final two nodes are merged node
    if not last_node_label:

        # add last edge to right childern of fake root
        # because the left childern of fake root is &amp;quot;TRUE root&amp;quot;, which has three childerns
        # the right childern of fake root is linked to left children of it in real
        # so the edge indictes the distance between left childern and right childern
        # even though right childern is linked to fake root in binary_tree
        TREEDICT[current_node_index[1]].key[1] = last_node_edges

        # create tree structure
        fake_root.left = TREEDICT[current_node_index[0]]
        fake_root.right = TREEDICT[current_node_index[1]]

    # if one of final two nodes is merged node
    else:

        # set node that is not merged node as tree structure
        right_node = TreeNode([last_node_label, last_node_edges])

        # set one of final two nodes, a merged node, as left childern of fake root
        fake_root.left = TREEDICT[current_node_index[0]]

        # add right childern
        fake_root.right = right_node

    return fake_root


def help_edge_matrix(tree, seqs: dict, edge_file: str):
    &amp;quot;&amp;quot;&amp;quot;[help function to do preorder traversal in order to get edge file]

    Args:
        tree ([Binary Tree])
        seqs (dict): [store information about label, seq, and index of seqs]
        edge_file (str): [output of edge]

    Returns:
        [Binary Tree]: [Binary Tree whose internal node has index]
    &amp;quot;&amp;quot;&amp;quot;
    # init index for internal node
    N = len(seqs) + 1
    # open new file
    EDGE_FILE = open(edge_file, &#39;w&#39;)  # changes

    def edge_matrix(tree):
        # get nonlocal value
        nonlocal N
        # preorder tree
        if tree:
            # index of fake root is None
            if tree.isRoot():

                tree.index = None
            # tree.key[0] is label of node
            # if it not in seqs then it is internal node
            elif (tree.key[0] not in seqs):
                # set index for internal node
                tree.index = N
                N += 1

            else:
                # get index of leaf node
                tree.index = seqs[tree.key[0]].index
            # write information of parent and chilren
            # left and right childern of fake root have no parent : None
            if tree.parent:

                EDGE_FILE.write(
                    f&#39;{tree.parent.index}\t{tree.index}\t{tree.key[1]}\n&#39;)
            edge_matrix(tree.left)
            edge_matrix(tree.right)

    edge_matrix(tree)

    # add the TRUE ROOT and its children
    EDGE_FILE.write(
        f&#39;{tree.left.index}\t{tree.right.index}\t{tree.right.key[1]}\n&#39;)

    EDGE_FILE.close()
    return tree


##############################################
#                         POSTORDER TREE
# WARNING: MUST CONDUCT AFTER PREODER
##############################################


def help_newick_parse(tree, tree_file: str):
    &amp;quot;&amp;quot;&amp;quot;[help function to do postorder traversal in order to get tree file]

    Args:
        tree ([Binary Tree])
        tree_file (str): [output of tree]
    &amp;quot;&amp;quot;&amp;quot;
    # open tree file
    TREE_FILE = open(tree_file, &#39;w&#39;)
    # get true root lable (left children of fake root)
    true_root_label = tree.left.key[0]

    def newick_parse(tree):
        # get nonlocal value
        nonlocal true_root_label
        # postorder tree
        if tree:

            left = newick_parse(tree.left)
            right = newick_parse(tree.right)
            # if node is internal node
            if left and right:
                # check if node is true node
                if tree.key[0] == true_root_label:
                    # not add &#39;( )&#39; so as to have three childern finally
                    return f&#39;{left},{right}&#39;
                # check if node is root
                elif tree.isRoot():
                    # add &#39;()&#39; finally
                    return f&#39;({left},{right})&#39;
                # if node is internal node
                else:
                    return f&#39;({left},{right}):{tree.key[1]}&#39;
            # if node is leaf node
            else:
                return f&#39;{tree.key[0]}:{tree.key[1]}&#39;

    newick = newick_parse(tree)

    TREE_FILE.write(f&#39;{newick};&#39;)

    TREE_FILE.close()


##############################################
#                         BOOTSTRAP
##############################################


def bootstrap(original_seqs: dict):
    &amp;quot;&amp;quot;&amp;quot;[bootstrap original seqs and return bootstrap sample and bootstrap seqs]

    Args:
        original_seqs ([dict]): [original seqs]

    Returns:
        tree_root_with_node_index: bootstrap sample whose node has index
        seqs_bootstrap : bootstrap seqs contained information like index, label, and sequences
    &amp;quot;&amp;quot;&amp;quot;
    # change data structure in oder to use pandas:
    #               0 1 2  (column_number)
    #label 1
    #label 2
    #...
    seqs_pd = {
        index: list(value.seq)
        for index, value in original_seqs.items()
    }
    df = pd.DataFrame(seqs_pd).T

    # using random to resample
    df_bootstrap = df.loc[:, random.choices(range(df.shape[1]), k=df.shape[1])]

    # get bootstrap information
    seqs_bootstrap = original_seqs.copy()
    # change sequences to bootstrap sequences
    for index, value in df_bootstrap.iterrows():
        seqs_bootstrap[index] = seqs_bootstrap[index]._replace(
            seq=&#39;&#39;.join(value.values))

    # get nodes needed to be coudcted NJ
    leaf_set = set(seqs_bootstrap.keys())
    # calculate genetic_distance for bootstrap samples
    distance_map = {}
    for seq1_label, seq1_info in seqs_bootstrap.items():

        for seq2_label, seq2_info in seqs_bootstrap.items():

            if seq1_label != seq2_label:

                distance = fetch_distance(seq1_label, seq2_label, distance_map)

                if not distance:

                    distance = genetic_distance(seq1_info.seq, seq2_info.seq)

                    distance_map[(seq1_label, seq2_label)] = distance
    # conduct NJ
    TREEDICT, _, final_distance_matrix = help_nj(leaf_set, distance_map)
    # construct binary_tree
    # get edge file for  last bootstrap (NOT USED)
    tree_root = my_tree(TREEDICT, final_distance_matrix, seqs_bootstrap)
    tree_root_with_node_index = help_edge_matrix(tree_root, seqs_bootstrap,
                                                 &#39;last_bootstrap.edge&#39;)
    return tree_root_with_node_index, seqs_bootstrap


def help_find_partion(tree) -&amp;gt; dict:
    &amp;quot;&amp;quot;&amp;quot;[function to partion list for internal nodes in a binary tree]

    Args:
        tree ([binary_tree]): [binary_tree with fake root]

    Returns:
        [dict]: [node_index: partion list]
    &amp;quot;&amp;quot;&amp;quot;
    # init dict to store index and its partion list
    internal_node_partion = defaultdict(list)

    # postorder traversal to find leafs for every internal node
    def find_partion(tree):

        LIST = []
        # begin
        if tree:

            left = find_partion(tree.left)
            right = find_partion(tree.right)

            if tree.isLeaf():

                LIST.append(tree.index)

            else:
                # flatten list
                LIST.extend([*left, *right])

                internal_node_partion[tree.index].extend(
                    sorted([*left, *right]))

            return LIST

    # change partion list of fake root to true root
    find_partion(tree)
    internal_node_partion[tree.left.index] = internal_node_partion.pop(
        tree.index)

    return internal_node_partion


def get_bootstrap_value(bootstrap_number: int,
                        original_seqs: dict,
                        original_tree,
                        bootstrap_file: str = &#39;bootstrap.txt&#39;):
    &amp;quot;&amp;quot;&amp;quot;[function to integrate relation methods to get bootstrap values for original tree]

    Args:
        bootstrap_number (int): [number for bootstrap. default = None]
        original_seqs (dict): [information of original sequences]
        original_tree ([type]): [binary tree of original sequences]
        bootstrap_file (str): [output of bootstrap]
    &amp;quot;&amp;quot;&amp;quot;
    # init list to store bootstrap value for every internal node
    internal_node_bootstrap_value = [0] * (len(original_seqs) - 2)
    # get partion list of every internal node in original tree
    original_partion_dict = help_find_partion(original_tree)
    # init list to store dict contained bootstrap_partion and its node_index
    bootstrap_partion_list = []
    # begin to bootstrap and consruct binary tree

    for number in range(bootstrap_number):
        logging.info(f&#39;BOOTSTRAP NUMBER : {number+1}&#39;)
        bootstrap_tree, _ = bootstrap(original_seqs)
        bootstrap_partion_list.append(help_find_partion(bootstrap_tree))

    # compare partion list to update bootstrap value
    for internal_node_index, partion_conent in original_partion_dict.items():

        for bootstrap_partion in bootstrap_partion_list:

            if partion_conent in bootstrap_partion.values():

                internal_node_bootstrap_value[internal_node_index -
                                              len(original_seqs) - 1] += 1
    # change format for the bootstrap values
    result = [
        f&#39;{value/bootstrap_number}\n&#39;
        for value in internal_node_bootstrap_value
    ]
    # write to file
    with open(bootstrap_file, &#39;w&#39;) as file:
        file.writelines(result)


##############################################
#                                  MAIN
##############################################


def worker(args: dict):
    &amp;quot;&amp;quot;&amp;quot;[function to integrate all above method to conduct NJ and output tree and edge file]

    Args:
        args ([dict]): [parameters getting from command line]

    Returns:
        tree_root_with_index : binary tree whose node has index
        seq : information of sequences
    &amp;quot;&amp;quot;&amp;quot;
    # read fa file and parse it
    seqs = read_input(args.input)
    # get labels of sequences
    leaf_set = set(seqs.keys())
    # get pair genetic distance
    distance_matrix = get_distance_matrix(seqs, args.distance)
    # delete distances between same sequences
    distance_matrix = {
        item: value
        for item, value in distance_matrix.items() if value != 0
    }
    # get TREEDICT and distance of final two nodes
    TREEDICT, _, final_distance_matrix = help_nj(leaf_set, distance_matrix)
    # construct binary tree REMEMBER it has fake root
    # left childern of fake root is regarded as true root
    tree_root = my_tree(TREEDICT, final_distance_matrix, seqs)
    # get edge file and add index for every node in binary tree
    tree_root_with_index = help_edge_matrix(tree_root, seqs, args.edge)
    # get tree file REMEMBER it must conduct after getting edge files
    # because it need index of node
    help_newick_parse(tree_root_with_index, args.tree)

    return tree_root_with_index, seqs


def main():
    args = get_parser()

    with Timer() as t:
        logging.info(f&#39;Neighbor Joining begin.......&#39;)
        tree_root_with_index, seqs = worker(args)

    logging.info(f&#39;Neighbor Joining  DONE : TIME {t.elapsed:.2f}s\n&#39;)

    if args.bootstrap:
        with Timer() as t:
            get_bootstrap_value(args.bootstrap, seqs, tree_root_with_index)
        logging.info(f&#39;BOOTSTRAP IS DONE : TIME {t.elapsed:.2f}s\n&#39;)
    else:
        logging.info(f&#39;BOOTSTRAP IS NOT DONE&#39;)

    logging.info(f&#39;EDGE_FILE : {args.edge}&#39;)
    logging.info(f&#39;TREE_FILE :  {args.tree}&#39;)
    logging.info(f&#39;DISTANCE_FILE: {args.distance}&#39;)


if __name__ == &amp;quot;__main__&amp;quot;:
    main()

&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;Thanks for your reading! Hopefully helpful!&lt;/strong&gt;&lt;/p&gt;
</description>
    </item>
    
    <item>
      <title>Slides</title>
      <link>https://cauliyang.github.io/en/slides/example/</link>
      <pubDate>Tue, 05 Feb 2019 00:00:00 +0000</pubDate>
      <guid>https://cauliyang.github.io/en/slides/example/</guid>
      <description>&lt;h1 id=&#34;create-slides-in-markdown-with-wowchemy&#34;&gt;Create slides in Markdown with Wowchemy&lt;/h1&gt;
&lt;p&gt;&lt;a href=&#34;https://wowchemy.com/&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;Wowchemy&lt;/a&gt; | &lt;a href=&#34;https://owchemy.com/docs/managing-content/#create-slides&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;Documentation&lt;/a&gt;&lt;/p&gt;
&lt;hr&gt;
&lt;h2 id=&#34;features&#34;&gt;Features&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;Efficiently write slides in Markdown&lt;/li&gt;
&lt;li&gt;3-in-1: Create, Present, and Publish your slides&lt;/li&gt;
&lt;li&gt;Supports speaker notes&lt;/li&gt;
&lt;li&gt;Mobile friendly slides&lt;/li&gt;
&lt;/ul&gt;
&lt;hr&gt;
&lt;h2 id=&#34;controls&#34;&gt;Controls&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;Next: &lt;code&gt;Right Arrow&lt;/code&gt; or &lt;code&gt;Space&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;Previous: &lt;code&gt;Left Arrow&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;Start: &lt;code&gt;Home&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;Finish: &lt;code&gt;End&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;Overview: &lt;code&gt;Esc&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;Speaker notes: &lt;code&gt;S&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;Fullscreen: &lt;code&gt;F&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;Zoom: &lt;code&gt;Alt + Click&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&#34;https://github.com/hakimel/reveal.js#pdf-export&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;PDF Export&lt;/a&gt;: &lt;code&gt;E&lt;/code&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;hr&gt;
&lt;h2 id=&#34;code-highlighting&#34;&gt;Code Highlighting&lt;/h2&gt;
&lt;p&gt;Inline code: &lt;code&gt;variable&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;Code block:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;porridge = &amp;quot;blueberry&amp;quot;
if porridge == &amp;quot;blueberry&amp;quot;:
    print(&amp;quot;Eating...&amp;quot;)
&lt;/code&gt;&lt;/pre&gt;
&lt;hr&gt;
&lt;h2 id=&#34;math&#34;&gt;Math&lt;/h2&gt;
&lt;p&gt;In-line math: $x + y = z$&lt;/p&gt;
&lt;p&gt;Block math:&lt;/p&gt;
&lt;p&gt;$$
f\left( x \right) = ;\frac{{2\left( {x + 4} \right)\left( {x - 4} \right)}}{{\left( {x + 4} \right)\left( {x + 1} \right)}}
$$&lt;/p&gt;
&lt;hr&gt;
&lt;h2 id=&#34;fragments&#34;&gt;Fragments&lt;/h2&gt;
&lt;p&gt;Make content appear incrementally&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;{{% fragment %}} One {{% /fragment %}}
{{% fragment %}} **Two** {{% /fragment %}}
{{% fragment %}} Three {{% /fragment %}}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Press &lt;code&gt;Space&lt;/code&gt; to play!&lt;/p&gt;
&lt;p&gt;&lt;span class=&#34;fragment &#34; &gt;
One
&lt;/span&gt;
&lt;span class=&#34;fragment &#34; &gt;
&lt;strong&gt;Two&lt;/strong&gt;
&lt;/span&gt;
&lt;span class=&#34;fragment &#34; &gt;
Three
&lt;/span&gt;&lt;/p&gt;
&lt;hr&gt;
&lt;p&gt;A fragment can accept two optional parameters:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;class&lt;/code&gt;: use a custom style (requires definition in custom CSS)&lt;/li&gt;
&lt;li&gt;&lt;code&gt;weight&lt;/code&gt;: sets the order in which a fragment appears&lt;/li&gt;
&lt;/ul&gt;
&lt;hr&gt;
&lt;h2 id=&#34;speaker-notes&#34;&gt;Speaker Notes&lt;/h2&gt;
&lt;p&gt;Add speaker notes to your presentation&lt;/p&gt;
&lt;pre&gt;&lt;code class=&#34;language-markdown&#34;&gt;{{% speaker_note %}}
- Only the speaker can read these notes
- Press `S` key to view
{{% /speaker_note %}}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Press the &lt;code&gt;S&lt;/code&gt; key to view the speaker notes!&lt;/p&gt;
&lt;aside class=&#34;notes&#34;&gt;
  &lt;ul&gt;
&lt;li&gt;Only the speaker can read these notes&lt;/li&gt;
&lt;li&gt;Press &lt;code&gt;S&lt;/code&gt; key to view&lt;/li&gt;
&lt;/ul&gt;

&lt;/aside&gt;
&lt;hr&gt;
&lt;h2 id=&#34;themes&#34;&gt;Themes&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;black: Black background, white text, blue links (default)&lt;/li&gt;
&lt;li&gt;white: White background, black text, blue links&lt;/li&gt;
&lt;li&gt;league: Gray background, white text, blue links&lt;/li&gt;
&lt;li&gt;beige: Beige background, dark text, brown links&lt;/li&gt;
&lt;li&gt;sky: Blue background, thin dark text, blue links&lt;/li&gt;
&lt;/ul&gt;
&lt;hr&gt;
&lt;ul&gt;
&lt;li&gt;night: Black background, thick white text, orange links&lt;/li&gt;
&lt;li&gt;serif: Cappuccino background, gray text, brown links&lt;/li&gt;
&lt;li&gt;simple: White background, black text, blue links&lt;/li&gt;
&lt;li&gt;solarized: Cream-colored background, dark green text, blue links&lt;/li&gt;
&lt;/ul&gt;
&lt;hr&gt;

&lt;section data-noprocess data-shortcode-slide
  
      
      data-background-image=&#34;/media/boards.jpg&#34;
  &gt;

&lt;h2 id=&#34;custom-slide&#34;&gt;Custom Slide&lt;/h2&gt;
&lt;p&gt;Customize the slide style and background&lt;/p&gt;
&lt;pre&gt;&lt;code class=&#34;language-markdown&#34;&gt;{{&amp;lt; slide background-image=&amp;quot;/media/boards.jpg&amp;quot; &amp;gt;}}
{{&amp;lt; slide background-color=&amp;quot;#0000FF&amp;quot; &amp;gt;}}
{{&amp;lt; slide class=&amp;quot;my-style&amp;quot; &amp;gt;}}
&lt;/code&gt;&lt;/pre&gt;
&lt;hr&gt;
&lt;h2 id=&#34;custom-css-example&#34;&gt;Custom CSS Example&lt;/h2&gt;
&lt;p&gt;Let&amp;rsquo;s make headers navy colored.&lt;/p&gt;
&lt;p&gt;Create &lt;code&gt;assets/css/reveal_custom.css&lt;/code&gt; with:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&#34;language-css&#34;&gt;.reveal section h1,
.reveal section h2,
.reveal section h3 {
  color: navy;
}
&lt;/code&gt;&lt;/pre&gt;
&lt;hr&gt;
&lt;h1 id=&#34;questions&#34;&gt;Questions?&lt;/h1&gt;
&lt;p&gt;&lt;a href=&#34;https://github.com/wowchemy/wowchemy-hugo-modules/discussions&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;Ask&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;&lt;a href=&#34;https://wowchemy.com/docs/managing-content/#create-slides&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;Documentation&lt;/a&gt;&lt;/p&gt;
</description>
    </item>
    
    <item>
      <title></title>
      <link>https://cauliyang.github.io/en/admin/config.yml</link>
      <pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate>
      <guid>https://cauliyang.github.io/en/admin/config.yml</guid>
      <description></description>
    </item>
    
  </channel>
</rss>
